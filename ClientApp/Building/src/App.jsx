
import React, { Component } from "react";
import { Route } from "react-router";
import { hot } from "react-hot-loader";

import "../../css/BuildingApp.css";

class App extends Component {
    render(){
        return(
            <div>building app</div>
        );
    }
}

export default hot(module)(App);